#include<bits/std++.h>
using namespace std;
//12.MULTI MAP.
//it stores duplicate keys in sorted order.

void explainMultimap()
{
	//everything same as map,only it can store multiple keys.
	//only map[key] cannot be used here.//{1,2}
	                                    //{1,3}
}
