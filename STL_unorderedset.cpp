#include<bits/stdc++.h>
using namespace std;
//10.UNORDERED SET;
//it stores elements unique but unsorted;that means we will store elements in different way not in order.
void explainUnorderedSet()
{
	unordered_set<int>st;
	//lower_bound and upper_bound function does not works
	//rest all functions are same as above,it does not stores in any particular order it has a better complexity
	//than set in most cases,except some when collision happens.
}